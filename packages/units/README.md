# `@grudge-studio/units`

NPC + unit schemas and default catalogs for **Forge editor** and game deployers.

```ts
import { DEFAULT_UNIT_CATALOG, getNpcDef, type UnitSpawnSpec } from "@grudge-studio/units";
```

Characters use the same motion contract as `@grudge-studio/character` (strip hip position, Box3 feet).
