# @grudge-studio/sdk

```bash
npm install @grudge-studio/sdk three
```

```ts
import {
  buildDefaultLoginUrl,
  grudge6RaceUrl,
  AnimationDirector,
  createEngineBoot,
  createDefaultManifest,
  buildFleetSatelliteRewrites,
  ASSET_BEST_PRACTICES,
} from "@grudge-studio/sdk";
```

**Deploy / editor app:** https://forge.grudge-studio.com
