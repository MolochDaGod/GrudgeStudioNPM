# @grudge-studio/engine

Engine manifest, boot probe, **HUMAN_CCT** / `configureRapierCharacterController`, playtest hosts, terrain sample helpers.

Used by fleet games and **forge.grudge-studio.com** as the shared engine contract.

Walk = Rapier CCT (host owns WASM). This package does not replace `Controller.ts`.

Playtest: `PLAYTEST_WITH_CONTROLLER` — start at https://open.grudge-studio.com/danger
