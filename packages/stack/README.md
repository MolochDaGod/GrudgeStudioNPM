# @grudge-studio/stack

Vercel rewrites, env keys, deploy surfaces. **Forge** is the npm deployment app.

```ts
import { buildFleetSatelliteRewrites, DEPLOY_SURFACES } from "@grudge-studio/stack";
```
