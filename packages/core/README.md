# @grudge-studio/core

Fleet URLs, Grudge ID login helpers, token keys.

```ts
import { buildDefaultLoginUrl, getFleetUrls, FLEET_GAME_ORIGINS } from "@grudge-studio/core";
window.location.href = buildDefaultLoginUrl("genesis");
```
